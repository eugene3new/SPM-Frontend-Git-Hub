import { ComponentStory, ComponentMeta } from "@storybook/react";
import ButtonMultiline from ".";

export default {
  title: "Core Components/Buttons/ButtonMultiline",
  component: ButtonMultiline,
  argTypes: {},
} as ComponentMeta<typeof ButtonMultiline>;

const Template: ComponentStory<typeof ButtonMultiline> = (args) => (
  <ButtonMultiline {...args} />
);

export const Primary = Template.bind({});
Primary.args = {
  type: "primary",
  align: "end",
  onClick: () => alert("Primary ButtonMultiline Clicked :D"),
  children: "PRIMARY",
  subText: "Enterprise Stakeholders",
};

export const Secondary = Template.bind({});
Secondary.args = {
  type: "secondary",
  onClick: () => alert("Secondary ButtonMultiline Clicked :/"),
  children: "SECONARY",
  subText: "-",
};
