import React, { FC, ReactNode } from "react";
import { ProgressGroupContainer } from "./progressGroupStyles";

export interface ProgressGroupProps {
  children: ReactNode;
}

export const ProgressGroup: FC<ProgressGroupProps> = ({
  children,
}: ProgressGroupProps) => {
  return <ProgressGroupContainer>{children}</ProgressGroupContainer>;
};

export default ProgressGroup;
