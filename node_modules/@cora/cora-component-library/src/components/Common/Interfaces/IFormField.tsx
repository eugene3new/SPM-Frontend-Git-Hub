import { Validation } from "../../Validation/validation";

export interface IFormField {
  id?: string;
  label: string;
  name?: string;
  value?: string;
  helpText?: string;
  type?: string;
  validation?: Validation;
  disabled?: boolean;
}
