import { ComponentStory, ComponentMeta } from "@storybook/react"

import { FileUpload } from "."

export default {
  title: "Page Components/FileUpload",
  component: FileUpload,
  argTypes: {},
} as ComponentMeta<typeof FileUpload>

const Template: ComponentStory<typeof FileUpload> = (args) => <FileUpload />

export const Default = Template.bind({})
Default.args = {
  title: "This is the modal title",
}

export const Info = () => {
  return (
    <FileUpload>
      The format of your CSV file is important. Ensure your table columns match{" "}
      <a href="/template-file.txt" className="upload-info-sample">
        this sample file
      </a>
    </FileUpload>
  )
}
