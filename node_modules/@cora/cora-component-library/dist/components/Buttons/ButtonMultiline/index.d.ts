import { FC, ReactNode } from "react";
export interface ButtonProps {
    children?: ReactNode;
    type?: "primary" | "secondary";
    align?: "start" | "end";
    subText?: string | "";
    onClick?: () => void;
    disabled?: boolean;
}
export declare const ButtonMultiline: FC<ButtonProps>;
export default ButtonMultiline;
