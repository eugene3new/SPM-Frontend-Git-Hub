import scss from "rollup-plugin-scss";
import typescript from "rollup-plugin-typescript2";

import pkg from "./package.json";

export default {
  input: "src/index.tsx",
  output: [
    {
      file: `${pkg.rollup}/index.js`,
      format: "cjs",
      exports: "named",
      sourcemap: true,
      strict: false,
    },
  ],
  plugins: [
    scss({
      output: `${pkg.rollup}/css/index.css`,
      sourceMap: true,
      outputStyle: "compressed",
    }),
    typescript(),
  ],
  external: ["react", "react-dom", "query-string"],
};
